"""
Unit tests for LGD Calculator (Dynamic).
Note: LGD Calculator was moved to perda_esperada module.
These tests are skipped as the module was restructured.
"""

import pytest

# Skip all tests in this module - LGD was moved to perda_esperada
pytestmark = pytest.mark.skip(reason="LGD Calculator moved to perda_esperada.src.modulo_lgd_segmentado")
